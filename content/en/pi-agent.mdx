---
title: "Pi Agent — Chat with Pi from WeChat"
description: "Pi extension that bridges WeChat to your Pi coding agent. Scan QR, send text/images/files/voice from your phone."
toc:
  - label: "How it works"
    href: "#how-it-works"
  - label: "Install"
    href: "#install"
  - label: "Usage"
    href: "#usage"
  - label: "Message flow"
    href: "#message-flow"
  - label: "Media support"
    href: "#media-support"
  - label: "Commands"
    href: "#commands"
  - label: "Pi APIs used"
    href: "#pi-apis"
  - label: "Features"
    href: "#features"
---

A **pi extension** that bridges WeChat to your [Pi coding agent](https://github.com/badlogic/pi-mono). Scan a QR code in WeChat → chat with Pi from your phone. Supports text, images, files, voice, and video. Uses the `@wechatbot/wechatbot` SDK for all iLink protocol operations.

<Section id="how-it-works" title="How it works">

<CodeBlock lang="diagram" lineHeight="1.3" showLineNumbers={false}>
{`┌──────────────┐      ┌──────────────┐      ┌──────────┐
│  WeChat User │ ←──→ │  iLink API   │ ←──→ │ Pi Agent │
│  (phone)     │      │  (Tencent)   │      │ (laptop) │
└──────────────┘      └──────────────┘      └──────────┘
                              ↑
                   @wechatbot/wechatbot SDK`}
</CodeBlock>

1. You run `/wechat` in pi
2. Extension creates a `WeChatBot` instance → calls `bot.login()`
3. SDK calls iLink API → gets a QR code URL → displayed in pi terminal
4. You scan the QR with WeChat on your phone
5. **WeChat messages → `pi.sendUserMessage()`** → become pi prompts
6. **`pi.on('agent_end')` → `bot.reply()`** → sent back to WeChat
7. Typing indicator shown while Pi thinks

</Section>

<Section id="install" title="Install">

**From npm (recommended):**

```bash
pi install npm:@wechatbot/pi-agent
```

**Or load directly (no install):**

```bash
pi -e npm:@wechatbot/pi-agent
```

**Or copy to extensions directory (auto-loads):**

```bash
cp -r pi-agent/ ~/.pi/agent/extensions/wechat-bridge/
```

</Section>

<Section id="usage" title="Usage">

```bash
> /wechat

  📱 Scan this QR code in WeChat:

    ▄▄▄▄▄▄▄ ▄▄▄ ▄▄▄▄▄▄▄
    █ ▄▄▄ █ █▀█ █ ▄▄▄ █
    ...

[wechat] QR scanned — confirm in WeChat
[wechat] Login confirmed
✓ WeChat connected! Account: e06c1ceea05e@im.bot
```

Now send a message from WeChat — Pi receives it as a prompt, processes it, and sends the reply back.

</Section>

<Section id="message-flow" title="Message flow">

<CodeBlock lang="diagram" lineHeight="1.3" showLineNumbers={false}>
{`WeChat user sends "帮我重构 auth 模块"
         │
         ▼
  iLink API (long-poll getupdates)
         │
         ▼
  Extension receives IncomingMessage
         │
         ├── bot.sendTyping(userId)        → "对方正在输入中..."
         │
         ├── pi.sendUserMessage(content)   → becomes pi prompt
         │
         ▼
  Pi agent processes (tools, thinking...)
         │
         ▼
  pi.on('agent_end') fires
         │
         ├── Extract assistant text
         ├── stripMarkdown()               → clean for WeChat
         ├── Detect generated file paths   → send as media
         ├── bot.stopTyping(userId)
         └── bot.reply(msg, content)       → sent back to WeChat`}
</CodeBlock>

</Section>

<Section id="media-support" title="Media support">

The extension handles all WeChat message types — not just text.

<ComparisonTable
  title="Inbound (WeChat → Pi)"
  headers={['Type', 'How it\'s handled']}
  rows={[
    ['Text', 'Sent directly as pi prompt'],
    ['Image', 'Downloaded → base64 → sent as ImageContent for vision models'],
    ['Voice', 'Uses WeChat transcription if available; otherwise downloads SILK audio'],
    ['File', 'Text files (.txt, .md, .py, etc): content included in prompt. Others: described with metadata'],
    ['Video', 'Downloaded → saved to temp file → pi told the file path for processing'],
  ]}
/>

<ComparisonTable
  title="Outbound (Pi → WeChat)"
  headers={['Type', 'How it\'s handled']}
  rows={[
    ['Text', 'Markdown stripped → sent as plain text'],
    ['Generated files', 'Detected in response → auto-sent via bot.reply({ file })'],
    ['Auto-routing', '.png → image message, .mp4 → video message, .pdf → file attachment'],
  ]}
/>

</Section>

<Section id="commands" title="Commands">

```
/wechat              Start login (show QR code)
/wechat --force      Force re-login (new QR code)
/wechat-disconnect   Disconnect WeChat
/wechat-send <text>  Manually send text to WeChat user
```

When already connected, `/wechat` shows a menu: Disconnect, Show status, or Cancel.

</Section>

<Section id="pi-apis" title="Pi APIs used">

<ComparisonTable
  title="Extension API usage"
  headers={['Pi API', 'Purpose', 'When']}
  rows={[
    ['pi.registerCommand()', '/wechat, /wechat-send, /wechat-disconnect', 'Extension load'],
    ['pi.sendUserMessage()', 'WeChat msg → pi prompt (text or image+text)', 'On each message'],
    ['pi.on(agent_end)', 'Capture response → stripMarkdown → send to WeChat', 'After agent finishes'],
    ['pi.on(message_update)', 'Track streaming text', 'During streaming'],
    ['ctx.ui.setStatus()', 'Connection status in footer', 'Throughout'],
    ['pi.on(session_shutdown)', 'Cleanup on exit', 'On shutdown'],
  ]}
/>

</Section>

<Section id="features" title="Features">

- Zero config — just `/wechat` and scan
- **Full media support** — images (vision), voice, files, video
- **Markdown stripping** — AI output → clean plain text for WeChat
- **Auto media routing** — generated `.png` sent as image, `.mp4` as video, etc.
- Credential persistence — `~/.wechatbot/`, skips QR on restart
- Session recovery — auto re-login on `-14`
- Typing indicator — "对方正在输入中" while Pi thinks
- Text chunking — long replies split at 2000 chars
- Graceful shutdown — stops on `session_shutdown`

</Section>
